#!/bin/sh
OPTS=
HTTPOPTS=
HOUSEOPTS=
OTHEROPTS=
if [ -e /etc/default/housegeneric ] ; then . /etc/default/housegeneric ; fi
if [ -e /etc/default/houserail ] ; then . /etc/default/houserail ; fi
sv start houseportal || exit 1
exec /usr/local/bin/houserail $HTTPOPTS $HOUSEOPTS $OTHEROPTS $OPTS

